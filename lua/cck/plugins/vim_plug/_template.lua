-- ===========================================================================
-- 
-- ===========================================================================
-- see: <>
-- see: `:help `

-- MEMO: Delete/disable/reverse this once plugin configuration is complete.
if not vim.env.NVIM_DEV_MODE then return end

error("MEMO: Add require().setup() call to plugin config!")
-- require('').setup({
-- })
